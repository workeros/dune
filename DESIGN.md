# Dune Web 设计规范

本规范面向日常编码工作台。实现使用 Rspack、React、Tailwind CSS 和 shadcn/ui，不使用 React Server Components。图标使用 Iconify 的 Remix Icons。

## 视觉方向

像把小时候画在笔记本上的世界剪下来，放进浏览器。以纸张、铅笔线、儿童涂鸦和轻柔的移动感建立亲切、安定、柔软的体验。手作感来自略不规则的轮廓、自然的笔画与少量剪贴装饰，页面保持清晰、安静，适合长时间工作。

暖纸色工作台，深灰绿文字与铅笔描边，珊瑚色主操作、薄荷色在线状态。纸卡、手写批注和涂鸦用作视觉点缀；终端、对话、表单和列表保持端正。以真实操作为主，首页不放营销大标题或虚构的机器与任务。安全感也来自可预期的操作、明确的状态和失败反馈。

## 品牌 Logo

登录页与工作台侧栏复用 [Brand 组件](web/src/components/brand.tsx)，样式统一维护在 [styles.css](web/src/styles.css)。

- 字标参考《Dune》电影的几何构形，以同一组半圆弧与延长笔画旋转组成 D、U、N、E，开口依次朝左、上、下、右。四个方向呼应完整圆形，字母之间保持宽阔留白。
- 使用本地 SVG 路径，不依赖远程图片或字体。深墨色到暖铜色的细微渐变表现弧线层次；E 省去中横，以横向渐隐光芒与微小行星亮边替代，在暖纸底色上保持轮廓清楚。
- Logo 以字标为主体，不再附加沙丘纸贴、手绘下划线或胶带装饰。preview 是下方独立的小号大写批注，使用界面字体与宽字距。
- Logo 保持静态品牌展示语义，不添加悬停旋转或光芒动画。整体提供 `Dune · Preview` 可访问名称，内部装饰对屏幕阅读器隐藏，避免重复朗读；多个实例的 SVG 渐变标识彼此独立。
- 登录页与侧栏复用同一组件；窄侧栏和手机宽度使用紧凑尺寸，保持四个开口与光芒完整，不挤占机器切换与工作区。

## 颜色与材质

| Token | 色值 | 用途 |
| --- | --- | --- |
| ink | `#35433e` | 正文、边框、图标 |
| muted | `#61716a` | 次要文字，浅底上保持可读 |
| paper | `#fbf4dd` | 工作台背景 |
| paper-deep | `#f3e8c7` | 侧栏、分区底色 |
| cream | `#fffaf0` | 表单、会话、对话纸卡 |
| coral | `#ef887e` | 主操作和当前选中标记 |
| coral-deep | `#ad4039` | 错误文字、焦点强调 |
| blue | `#91c6d8` | 辅助信息 |
| mint | `#afd7b7` | 在线、成功 |
| yellow | `#f1d477` | 等待授权、注意事项 |
| lavender | `#c7b6d8` | ACP 类型辅助标记 |

共享 CSS 变量映射到 shadcn 语义 token。细线分隔使用 ink 的低透明度；纸卡使用 1–1.5px 描边、8–12px 圆角和低透明度的轻微偏移阴影。纸张以暖色平涂为主，胶带与彩纸可叠加半透明颜色；不规则边缘和铅笔排线集中在品牌装饰中，不影响控件边界与文字对比度。

横线纸仅用于大面积空白背景，不覆盖终端和正文。涂鸦保持少量、留白充分，不用高密度噪点或纹理干扰阅读。不要仅以颜色表达状态，必须配文字或图标。

页面背景可沿用手绘语言加入原创沙漠点缀：下缘使用低透明度沙丘和铅笔排线，远处沙虫只露出小幅轮廓，漂沙使用少量短笔触。装饰位于内容层之后，不接收指针事件，不覆盖表单、工作区或终端；窄屏减少沙粒数量。漂沙、沙虫摆动与太阳光线旋转都应缓慢而克制，`prefers-reduced-motion` 下完全静止。

## 字体与密度

正文使用 `ui-rounded, "SF Pro Rounded", system-ui, sans-serif`，主要正文 16px、操作标签 14px、次要元信息不小于 12px。代码与终端使用 `"SFMono-Regular", Consolas, monospace`，默认 14px。品牌字标使用几何路径，短批注使用界面字体；手写字体不用于命令、错误或任务内容。使用本机字体回退，不依赖远程字体服务。

## 页面与组件

- 登录页为紧凑纸卡，直接提供登录、注册和明确错误。
- 主工作台由固定宽度环境/会话侧栏、顶部项目与操作条、可伸缩主工作区组成。首屏就能添加机器或开始会话。
- 环境列表使用逻辑 Runner。点击环境固定其当前绑定；刷新不自动切到新绑定，变化时关闭旧工作区并提供明确进入当前环境的操作。未绑定环境不展示可执行工作区，进入环境后仍需选择已有会话。
- 空状态展示真实的下一步操作；离线、启动中、执行中、等待授权、已退出分别呈现。加载失败不能显示为无数据。
- 表单、按钮、弹窗使用本地 shadcn/ui 组件，沿用其键盘与焦点行为。自定义样式通过 token 和 class 完成。
- Agent 配置展示名称、PTY/ACP 模式、启动命令、参数、环境变量和工作目录。命令未安装也能保存，执行失败时给出实际错误。
- PTY 保持终端原始 ANSI 色彩与交互语义；历史加载区域与当前终端操作分开，滚动历史不向 Agent 误发按键。
- ACP 用户输入与 Agent 回复使用简洁的纸卡层级；工具活动可展开。待授权是明确的操作卡，不自动确认。
- Git diff 使用等宽字体、明确的增删标记和独立滚动容器。
- 功能图标统一通过 Iconify 渲染 Remix Icons，随前端打包，不要求运行时访问图标 CDN。品牌 Logo 与装饰涂鸦使用本地 SVG，不替代功能图标。仅图标按钮必须有可访问名称。

## 响应式与交互

桌面优先。宽度不足时将侧栏转为紧凑顶部区域，保留机器/会话切换入口，避免工作区被挤到不可操作。横向浏览的轻盈感用于窄屏机器与会话列表，优先支持原生触摸滑动、触控板滚动和键盘聚焦，并让下一项的边缘提示内容仍可继续浏览。若后续增加鼠标拖动，仅限明确的列表区域，保留点击与键盘操作，避免将拖动误判为选择。

不引入面向宣传页的全页横向翻页或全局方向键拦截。终端按键和文本选择优先；装饰移动不改变布局，也不遮挡操作入口。

按钮与菜单反馈控制在 120–160ms，遵循 `prefers-reduced-motion`。焦点使用清晰的珊瑚色轮廓，表单关联 label，状态反馈使用合适的 live region。复制和提交失败必须呈现真实反馈。

## 验收

检查桌面宽屏、窄窗口、200% 文本缩放、键盘操作、弹窗焦点返回、长命令与长路径、离线与错误状态。没有后端数据时不以示例记录伪装真实机器；生产构建、类型检查和真实交互验证分别记录。

品牌改动同时检查登录页和侧栏：四个方向的弧形字标、星光与批注无裁切或重叠，小尺寸轮廓清楚，屏幕阅读器只朗读一次品牌名称，Logo 在普通及减少动态效果模式下均保持静态。视觉验收关注柔和配色、轻描边与留白，也确认装饰不会削弱正文、状态或焦点的辨识度。本规范描述设计要求，当前实现与实际验收范围以代码和本次验证记录为准。
