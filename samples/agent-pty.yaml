version: 1
kind: agent
working_directory: /tmp/dune-demo
env:
  TERM: xterm-256color
setup:
  steps:
    - name: check-codex
      argv: [codex, --version]
      timeout_seconds: 10
adapter: pty
start:
  argv: [codex]
